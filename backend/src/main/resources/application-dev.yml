spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sport_edu_mgmt?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3000ms
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /

logging:
  level:
    root: info
    com.sportedu.backend: info
    org.springframework.security: info

management:
  health:
    redis:
      enabled: false
    rabbit:
      enabled: false
